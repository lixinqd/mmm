慢慢买项目问题记录：
   通过ajax发送请求获取数据渲染页面，注意传参和模板引擎的绑定，比如通过传入id渲染在本地打开时需要传入默认值；

1 实现分页功能：省钱控页面有实现分页功能的具体代码（海淘折扣和省钱控共用页面）
     通过在数据info上添加数组或对象属性实现
2 封装获取地址栏参数的方法 在js文件中（getSearch.js）
3 白菜价页面中导航标题实现区域滚动功能：
  1)用到区域滚动插件iscroll.js
  2)基于jquery实现动态计算li的宽度并赋值给ul实现区域滚动
  3）点击标题渲染对应页面时：实现高亮要阻止a的默认跳转行为  
     通过a标签的跳转和点击时的渲染是不一样的（注册点击事件时也需要阻止a的默认行为）
4 百分比布局时问题：
  1）在宽度100%的父容器中放入不同比例的盒子 要用浮动或者定位才能放在一行
       因为盒子是块级元素独占一行